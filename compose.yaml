# This compose file must be in the users local directory, check this to avoid

services:
    base:
        image: "inakivazquez/deustorl:base3"
        container_name: "rltrain"
        privileged: true
        tty: true
        stdin_open: true
        volumes:
            - .:/home/rl/my_scripts

    linux-cpu:
        extends:
            service: rltrain-base
        network_mode: "host"
        privileged: true
        volumes:
            - /tmp/.X11-unix:/tmp/.X11-unix
        environment:
            - DISPLAY=$DISPLAY
            - SDL_VIDEODRIVER=x11

    linux-gpu:
        extends:
            service: rltrain-linux-cpu
        deploy:
            resources:
                reservations:
                    devices:
                        - capabilities: [gpu]
                          count: all

    windows-cpu:
        extends:
            service: rltrain-base
        ports:
            - "6006:6006"
        privileged: true

    windows-gpu:
        extends:
            service: rltrain-windows-cpu
        deploy:
            resources:
                reservations:
                    devices:
                        - capabilities: [gpu]
                          count: all
